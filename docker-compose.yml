version: '3.8'

services:
  forward-proxy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: forward-proxy
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
    restart: unless-stopped
    networks:
      - proxy-network

networks:
  proxy-network:
    driver: bridge

